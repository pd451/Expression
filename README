This repository contains the expression.java file, which implements a recursive two stack algorithm to evaluate a mathematical expression as written in a text file. Comments within the file explain the implementation and context. 
